services:
  - type: web
    name: penumbra-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 24
      - key: CLIENT_ORIGIN
        sync: false
